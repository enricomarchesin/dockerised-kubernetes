# Tutorial

```sh
pip install poetry
poetry new my-package
cd my-package
poetry install --no-root
poetry run pytest
poetry env list
poetry add python-cfonts
poetry show --tree
cfonts
poetry run cfonts
poetry shell
cfonts
poetry build
ls -la dist/
```
